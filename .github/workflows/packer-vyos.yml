# DEPRECATED: This Packer-based workflow has been replaced by vyos-build.yml
#
# The new vyos-build approach:
# - Uses official vyos/vyos-build Docker container
# - Bakes configuration directly into the image (no keystroke automation)
# - Doesn't require KVM/QEMU nested virtualization
# - Produces identical raw disk output for Tinkerbell/NAS deployment
#
# See: .github/workflows/vyos-build.yml
# See: infrastructure/network/vyos/vyos-build/README.md
#
# This file is preserved for historical reference only.

name: "[DEPRECATED] Build VyOS Image (Packer)"

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is deprecated. Use vyos-build.yml instead.'
        type: string
        default: 'deprecated'

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow deprecated
        run: |
          echo "This Packer-based workflow has been replaced by vyos-build.yml"
          echo ""
          echo "The new workflow uses the official vyos-build toolchain which:"
          echo "  - Doesn't require KVM/QEMU nested virtualization"
          echo "  - Bakes configuration directly into the image"
          echo "  - Is more reliable than keystroke-based automation"
          echo ""
          echo "To build VyOS images, use the 'Build VyOS Image' workflow instead."
          exit 1

# Historical reference - Packer-based build workflow:
#
#   validate:
#     runs-on: warp-ubuntu-latest-x64-8x
#     steps:
#       - uses: actions/checkout@v4
#
#       - uses: hashicorp/setup-packer@v3.1.0
#         with:
#           version: '1.11.2'
#
#       - name: Packer Init
#         working-directory: infrastructure/network/vyos/packer
#         run: packer init .
#
#       - name: Packer Validate
#         working-directory: infrastructure/network/vyos/packer
#         run: |
#           # Validate with dummy values for required vars without defaults
#           # Note: vyos_iso_url/checksum come from source.auto.pkrvars.hcl (auto-loaded)
#           packer validate \
#             -var "ssh_key_type=ssh-ed25519" \
#             -var "ssh_public_key=AAAA" \
#             .
#
#   build:
#     if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
#     runs-on: warp-ubuntu-latest-x64-8x
#     needs: validate
#     steps:
#       - uses: actions/checkout@v4
#
#       - uses: actions/setup-go@v5
#         with:
#           go-version: '1.23'
#           cache-dependency-path: tools/labctl/go.sum
#
#       - name: Build labctl
#         run: |
#           cd tools/labctl
#           go build -o ../../labctl .
#
#       - name: Install SOPS
#         run: |
#           curl -LO https://github.com/getsops/sops/releases/download/v3.9.2/sops-v3.9.2.linux.amd64
#           chmod +x sops-v3.9.2.linux.amd64
#           sudo mv sops-v3.9.2.linux.amd64 /usr/local/bin/sops
#
#       - name: Write SOPS age key
#         run: |
#           echo "${{ secrets.SOPS_AGE_KEY }}" > /tmp/age-key.txt
#           chmod 600 /tmp/age-key.txt
#
#       - name: Extract SSH public key
#         env:
#           SOPS_AGE_KEY_FILE: /tmp/age-key.txt
#         run: |
#           sops --decrypt \
#             --extract '["ssh_public_key"]' images/packer-ssh.sops.yaml > /tmp/ssh_key.pub
#
#       - name: Install QEMU
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y qemu-system-x86 qemu-utils
#
#       - name: Enable KVM access
#         run: |
#           echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
#           sudo udevadm control --reload-rules
#           sudo udevadm trigger --name-match=kvm
#           sudo usermod -aG kvm $USER
#           sudo chmod 666 /dev/kvm || true
#
#       - uses: hashicorp/setup-packer@v3.1.0
#         with:
#           version: '1.11.2'
#
#       - name: Packer Init
#         working-directory: infrastructure/network/vyos/packer
#         run: packer init .
#
#       - name: Packer Build
#         working-directory: infrastructure/network/vyos/packer
#         run: |
#           # Extract key type and body from public key
#           # Note: vyos_iso_url/checksum auto-loaded from source.auto.pkrvars.hcl
#           SSH_KEY_TYPE=$(awk '{print $1}' /tmp/ssh_key.pub)
#           SSH_KEY_BODY=$(awk '{print $2}' /tmp/ssh_key.pub)
#           packer build \
#             -var "ssh_key_type=${SSH_KEY_TYPE}" \
#             -var "ssh_public_key=${SSH_KEY_BODY}" \
#             .
#
#       - name: Upload to e2
#         run: |
#           ./labctl images upload \
#             --credentials images/e2.sops.yaml \
#             --sops-age-key-file /tmp/age-key.txt \
#             --source infrastructure/network/vyos/packer/output/vyos-lab.raw \
#             --destination vyos/vyos-gateway.raw
