#cloud-config
autoinstall:
  version: 1
  locale: en_US
  keyboard:
    layout: us
  identity:
    hostname: bootstrap-pxe
    username: packer
    # password: "packer" (SHA-512 crypt)
    password: "$6$packer$boWUDPn2ItbIVp75vZkcB9enktYcH/yND03ZqeO.xN1ydPY2A8ZRsbTDbbiRlToGQ97O4.AM3Tdw9FQoPk41k."
  ssh:
    install-server: true
    allow-pw: true
  storage:
    layout:
      name: direct
  package_update: true
  packages:
    - qemu-guest-agent
    - sudo
  late-commands:
    - curtin in-target --target=/target -- systemctl enable ssh
    - curtin in-target --target=/target -- systemctl enable qemu-guest-agent
    - curtin in-target --target=/target -- /bin/sh -c 'echo "packer ALL=(ALL) NOPASSWD:ALL" >/etc/sudoers.d/99-packer-nopasswd'
    - curtin in-target --target=/target -- chmod 440 /etc/sudoers.d/99-packer-nopasswd


